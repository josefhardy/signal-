# Copy this to .env and fill in real values. Never commit .env.

# Database (not wired up yet — MVP uses JSON-file storage, see services/storage.py)
DATABASE_URL=postgresql://user:password@localhost:5432/signal

# OpenAI key — used for both transcription (Whisper) and SEO generation (chat completions)
OPENAI_API_KEY=

# App
ENVIRONMENT=development
